import styled from 'styled-components'

const Page = styled.div`
    color: ${({ theme }) => theme.colors.white};
    display: flex;
    font-size: 20px;
    padding: 20px 40px;
`

export default Page;